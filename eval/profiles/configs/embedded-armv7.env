# Config-under-test: the WB7 controller profile (docker/Dockerfile.armv7's CONFIG_PROFILE).
# Normally exercised via TARGET=wb7 (already deployed on the controller), not brought up
# locally — listed so `make compare` can include it where the hardware/emulation allows.
IRENE_CONFIG_FILE=config/embedded-armv7.toml
