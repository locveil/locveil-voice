# UV virtual environment
.venv/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.flake8

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# celery beat schedule file
celerybeat-schedule

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Project specific
options/
temp/
tts_cache/
*.wav
# ...but eval audio fixtures are versioned test inputs (reproducible WER + runnable in CI), not stray audio.
# `**` so the per-language subdirs (eval/fixtures/<lang>/*.wav, I18N-5) are carved out too.
!eval/fixtures/**/*.wav
*.log

# Centralized logging - ignore all log files in logs folder but keep the directory
logs/*.log
logs/*.txt
logs/*.json

model/*.mdl
model/graph/*.fst
model/ivector/*.dubm
model/ivector/*.ie
model/ivector/*.mat

config-ui/node_modules/
config-ui/dist/
# Runtime cache (ClientRegistry persistence, model downloads, temp, tts)
cache/

# eval/ — generated results and local-only config profile
eval/results-*.json
eval/.env
eval/profiles/configs/custom.env
eval/profiles/recording.env
