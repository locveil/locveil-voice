# Config-under-test: the WB7 armv7 profile — ENGLISH (I18N-5). EVAL_LANG derives `en` from the `-en`
# CONFIG name, so the WS suite uses fixtures/en + English cases automatically.
IRENE_CONFIG_FILE=configs/embedded-armv7-en.toml
